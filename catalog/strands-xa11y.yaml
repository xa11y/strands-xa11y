# Draft submission for the Strands integrations catalog.
#
# To submit: fork the harness-sdk repository, copy this file to
# site/src/content/catalog/strands-xa11y.yaml, and open a PR using the
# "Integration submission" template. The package must be on PyPI first.
#
# Check the field shapes against the schema in that repo before opening the PR —
# this draft follows the documented fields, but the schema is the authority, and it
# rejects `maintainer`, `registry`, `featured`, and `badges` (the Strands team sets those).
name: strands-xa11y
description: >-
  Drive native macOS, Windows, and Linux desktop apps through the accessibility
  tree — structured UI snapshots, semantic actions, screenshots as fallback.
integrationType: tool
languages:
  python: strands-xa11y
github: https://github.com/xa11y/strands-xa11y
docsUrl: https://xa11y.dev/integrations/strands/
addedDate: '2026-08-10'
